

---

# 🧠 DOM Mastery Problem Set

---

# 🟡 Q1. Dynamic FAQ Accordion (State + DOM Traversal)

Create an FAQ section where:

### Requirements

* Questions are rendered from a JS array
* Clicking a question should:

  * Toggle only that answer
  * Close any previously open answer
* Use:

  * `createElement`
  * `appendChild`
  * `classList`
  * `nextElementSibling`
  * `querySelectorAll`
* Add `aria-expanded` attribute for accessibility

👉 Tests: DOM creation, traversal, state handling

---

# 🟡 Q2. Live Character Counter with Color Threshold

Build a textarea with:

### Behavior

* Show remaining characters (max: 200)
* Counter turns:

  * 🟢 green → above 100
  * 🟠 orange → 50–100
  * 🔴 red → below 50
* Disable submit button at 0 characters

### Must use

* `input` event
* `textContent`
* `style`
* `setAttribute` / `removeAttribute`

---












